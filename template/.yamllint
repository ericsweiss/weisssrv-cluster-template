---
# Shared by pre-commit, the CI yaml-lint job and `task lint` so all three agree.
# `relaxed` with line-length disabled — inline documentation comments and Flux
# substitution placeholders run long.
extends: relaxed

rules:
  line-length: disable
  document-start:
    present: true
    level: warning
