{%- set lib_url_ = lib_url | default('https://git.ericsweiss.com/eric/weisssrv-lib.git') -%}
{%- set lib_project = lib_project | default(lib_url_.split('://')[1].split('/', 1)[1] | replace('.git', '')) -%}
{%- set runner_tag = ci_runner_tag | default('infrastructure') -%}
{%- set cpu_selector = ci_cpu_selector | default(internal_domain ~ '/cpu=modern') -%}
{%- set cluster_dir = 'kubernetes/clusters/' ~ cluster_name -%}
{%- set versions_cm = 'kubernetes/infrastructure/sources/versions-configmap.yaml' -%}
{%- set config_cm = 'kubernetes/infrastructure/sources/cluster-config.yaml' -%}
---
# CI for the {{ cluster_name }} cluster. Generic lint / validate / security /
# review jobs are pulled from {{ lib_project }} at a pinned ref; only the deploy
# jobs and the wiring below are local. See docs/ci-pipeline.md for the runner,
# schedule and credential prerequisites.
#
# `include: project:` resolves on THIS GitLab instance, so the library must
# exist here as {{ lib_project }} — mirror or fork it if the cluster does not
# live on the instance that hosts it.
stages:
  - lint
  - validate
  - test
  - security
  - ai-review
  - deploy
  - maintenance
{%- if enable_semantic_release | default(false) %}
  - release
{%- endif %}

workflow:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main"'
    # version-bump-bot runs on a pipeline schedule; without this clause the
    # schedule creates no pipeline at all.
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
    - if: '$CI_PIPELINE_SOURCE == "web"'

variables:
  ANSIBLE_VERSION: "11.6.0"
  PYYAML_VERSION: "6.0.2"

include:
  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/lint/yaml-lint.yml
    inputs:
      tags: ["{{ runner_tag }}"]
      # -c .yamllint, not -d relaxed: the repo's config is what pre-commit and
      # `task lint` use, and a job that ignores it reports different findings.
      config: "-c .yamllint"
      targets: "ansible/ kubernetes/ terraform/ scripts/ .gitlab-ci.yml Taskfile.yml"
      changes:
        - "ansible/**/*"
        - "kubernetes/**/*"
        - "terraform/**/*"
        - "scripts/**/*.yml"
        - "Taskfile.yml"
        - ".yamllint"
        - ".gitlab-ci.yml"

  # collections_path is empty: this repo ships no in-repo collection, so the
  # only search path is the galaxy install of weisssrv.infra from requirements.yml.
  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/lint/ansible-lint.yml
    inputs:
      tags: ["{{ runner_tag }}"]
      targets: "ansible/"
      collections_path: ""
      galaxy_requirements: "ansible/requirements.yml"
      changes:
        - "ansible/**/*"
        - ".ansible-lint"
        - ".ansible-lint-ignore"
        - ".gitlab-ci.yml"

  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/lint/shellcheck.yml
    inputs:
      tags: ["{{ runner_tag }}"]
      direct_globs: "scripts/*.sh"
      find_dir: ""
      changes:
        - "scripts/**/*.sh"
        - ".gitlab-ci.yml"

  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/lint/python-lint.yml
    inputs:
      tags: ["{{ runner_tag }}"]
      image: python:3.13-slim
      targets: "scripts tests"
      changes:
        - "scripts/**/*.py"
        - "tests/**/*.py"
        - "ruff.toml"
        - "pyproject.toml"
        - ".gitlab-ci.yml"

  # tests/ holds the cluster invariants (substitution coverage, no site literals
  # in the Kubernetes tree). apt_packages is cleared and the FULL python image
  # used so the job stays runnable on a non-root runner.
  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/test/python-tests.yml
    inputs:
      tags: ["{{ runner_tag }}"]
      image: python:3.13
      apt_packages: ""
      test_dir: "tests"
      changes:
        - "tests/**/*"
        - "kubernetes/**/*"
        - "scripts/**/*.py"
        - ".gitlab-ci.yml"

  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/validate/terraform.yml
    inputs:
      tags: ["{{ runner_tag }}"]
      fmt_dir: "terraform/"
      module_glob: "terraform/*/"
      changes:
        - "terraform/**/*"
        - ".gitlab-ci.yml"

  # Substitute mode mirrors what kustomize-controller does at reconcile time:
  # both postBuild ConfigMaps are exported, every ${placeholder} in a rendered
  # Kustomization must resolve to one of their keys, and the result is
  # kubeconform'd. versions_configmap takes BOTH files (see scripts/flux-render.sh).
  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/validate/flux-lint.yml
    inputs:
      tags: ["{{ runner_tag }}"]
      substitute: true
      cluster_dir: "{{ cluster_dir }}"
      versions_configmap: "{{ versions_cm }} {{ config_cm }}"
      flux_render_script: "scripts/flux-render.sh"
      skipped_script: "scripts/kubeconform-skipped.py"
      changes:
        - "kubernetes/**/*"
        - "ansible/inventories/prod/group_vars/all.yml"
        - ".gitlab-ci.yml"

  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/security/secret-detection.yml
    inputs:
      tags: ["{{ runner_tag }}"]
      cpu_selector: "{{ cpu_selector }}"

  # env mode: the two keys come from masked CI/CD variables, so the job needs no
  # 1Password CLI and no root. The gate means a pipeline without them simply has
  # no AI review rather than a red job.
  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/review/pr-agent.yml
    inputs:
      tags: ["{{ runner_tag }}"]
      secrets_source: "env"
      gate: '$OPENAI__KEY && $GITLAB__PERSONAL_ACCESS_TOKEN'
      needs:
        - job: yaml-lint
          optional: true
        - job: ansible-lint
          optional: true
        - job: flux-lint
          optional: true
        - job: secret_detection
          optional: true

  # Weekly pin refresh. Runs ONLY from a pipeline schedule (or a manual web run)
  # and needs $VERSION_BUMP_BOT_TOKEN — see docs/ci-pipeline.md for both.
  #
  # check_command is the non-interactive twin of `task maintenance:update-all-versions`
  # (check-versions is not vendored; it is run from the pinned library checkout,
  # exactly as the Taskfile does). Keep the two in step — including
  # --nested-key, without which every helm_chart_versions_* key vanishes from
  # the regenerated ConfigMap. check-versions exits 1 when updates exist, which
  # would kill the job before the bot ever runs.
  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/maintenance/version-bump-bot.yml
    inputs:
      tags: ["{{ runner_tag }}"]
      setup_command: >-
        pip install --user --quiet pyyaml==$PYYAML_VERSION &&
        git clone --depth 1 --branch {{ lib_ref }} {{ lib_url_ }} .weisssrv-lib
      check_command: >-
        python3 .weisssrv-lib/scripts/check-versions.py --json > version-report.json || true;
        python3 .weisssrv-lib/scripts/check-versions.py --update-all || true;
        python3 scripts/generate-versions-configmap.py
        --vars-file ansible/inventories/prod/group_vars/all.yml
        --output {{ versions_cm }}
        --nested-key helm_chart_versions
        --regen-command "task flux:sync-versions"
      paths: "ansible/inventories/prod/group_vars/all.yml kubernetes/"
      report_path: "version-report.json"
      title: "chore(deps): {{ cluster_name }} version bumps"
{%- if enable_semantic_release | default(false) %}

  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/release/semantic-release.yml
    inputs:
      tags: ["{{ runner_tag }}"]
{%- endif %}

  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/templates/dep-cache.yml

  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/templates/install-1password.yml

  - project: {{ lib_project }}
    ref: {{ lib_ref }}
    file: /ci/templates/terraform-http-backend.yml
    inputs:
      # ${CI_API_V4_URL} tracks the instance the pipeline runs on; TF_HTTP_PASSWORD
      # is the job token, which is only valid there.
      api_url: "${CI_API_V4_URL}"
      state_name: "{{ dns_backend }}"

# ==============================================================================
# Deploy — starter set. One job per playbook, gated to main + a changes list, so
# a merge deploys exactly what it touched. Adding a playbook means adding a job:
# the pattern is documented in docs/ci-pipeline.md.
# ==============================================================================
.deploy-base:
  extends: [.install-1password, .dep-cache]
  # Deploys touch live infrastructure: never cancel one because a new pipeline
  # started. Runner-pod casualties are environmental and the playbooks are
  # idempotent, so retry those rather than failing the chain.
  interruptible: false
  retry:
    max: 2
    when:
      - runner_system_failure
  before_script:
    - !reference [.install-1password, before_script]
    - pip install --quiet "ansible==${ANSIBLE_VERSION}"
    - |
      # A world-writable checkout makes Ansible ignore ansible.cfg outright.
      export ANSIBLE_CONFIG="$(mktemp /tmp/ansible-XXXXXXXXXX.cfg)"
      install -m 600 ansible/ansible.cfg "$ANSIBLE_CONFIG"
      trap "rm -f '$ANSIBLE_CONFIG'" EXIT
    - |
      mkdir -p ~/.ssh
      chmod 700 ~/.ssh
      # umask in a subshell: the key file is never created 0644, so there is no
      # chmod race window.
      (umask 077; op read "op://{{ onepassword_vault }}/SSH Key/private key" > ~/.ssh/id_ed25519)
      # TOFU keyscan — ephemeral runners inherit no known_hosts. ALL_SSH_IPS is
      # generated from the inventory (scripts/hosts.env), so a new node is
      # covered without editing this file.
      . scripts/hosts.env
      # shellcheck disable=SC2086  # word-split ALL_SSH_IPS into one keyscan
      ssh-keyscan -H $ALL_SSH_IPS >> ~/.ssh/known_hosts 2>/dev/null || true
    - ansible-galaxy collection install -r ansible/requirements.yml

# The op:// variables mirror the matching Taskfile task's `env:` exactly — `op
# run` resolves EVERY op:// reference in the environment, so a job must declare
# what its playbook needs and nothing more (an item the vault lacks is a hard
# failure, not an unused variable).
deploy-ansible-base:
  stage: deploy
  extends: .deploy-base
  resource_group: deploy-ansible-base
  variables:
    ROOT_EMAIL_ALIAS: "op://{{ onepassword_vault }}/Email Config/root_alias"
    SSH_PUBLIC_KEY: "op://{{ onepassword_vault }}/SSH Key/public key"
  script:
    - cd ansible
    - op run -- ansible-playbook -i inventories/prod playbooks/base.yml
  environment:
    name: production/ansible-base
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - "ansible/inventories/**/*"
        - "ansible/playbooks/base.yml"
        - "ansible/playbooks/site.yml"
        - "ansible/requirements.yml"

# Manual: a k3s change can restart the control plane, so an operator triggers it
# after reading the diff.
deploy-ansible-k3s:
  stage: deploy
  extends: .deploy-base
  resource_group: deploy-ansible-k3s
  variables:
    ROOT_EMAIL_ALIAS: "op://{{ onepassword_vault }}/Email Config/root_alias"
    SSH_PUBLIC_KEY: "op://{{ onepassword_vault }}/SSH Key/public key"
    SMTP_RELAY_USER: "op://{{ onepassword_vault }}/SMTP Relay Auth/username"
    SMTP_RELAY_PASSWORD: "op://{{ onepassword_vault }}/SMTP Relay Auth/password"
    K3S_TOKEN: "op://{{ onepassword_vault }}/K3s Cluster Token/credential"
    # Agents join with the lower-privilege worker token the servers advertise.
    K3S_AGENT_TOKEN: "op://{{ onepassword_vault }}/K3s Agent Token/credential"
    LOKI_PUSH_USER: "op://{{ onepassword_vault }}/Loki Push Auth/username"
    LOKI_PUSH_PASSWORD: "op://{{ onepassword_vault }}/Loki Push Auth/password"
  script:
    - cd ansible
    - op run -- ansible-playbook -i inventories/prod playbooks/k3s.yml
  environment:
    name: production/k3s
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - "ansible/inventories/**/*"
        - "ansible/playbooks/k3s.yml"
        - "ansible/playbooks/k3s-provision-vms.yml"
      when: manual
      allow_failure: false

deploy-terraform:
  stage: deploy
  extends: .terraform-http-backend
  interruptible: false
  image:
    name: hashicorp/terraform:1.15
    entrypoint: [""]
  before_script:
    - !reference [.install-1password-alpine, before_script]
  script:
    - cd terraform/{{ dns_backend }}
    # -lockfile=readonly: provider hashes are committed; CI rewriting them would
    # mask supply-chain drift.
    - terraform init -lockfile=readonly
    - |
      export TF_VAR_cloudflare_api_token=$(op read "op://{{ onepassword_vault }}/Cloudflare Terraform Token/credential")
      export TF_VAR_cloudflare_account_id=$(op read "op://{{ onepassword_vault }}/Cloudflare Terraform Token/username")
      terraform apply -auto-approve -lock-timeout=120s
  environment:
    name: production/{{ dns_backend }}
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - "terraform/{{ dns_backend }}/**/*"
      when: manual
      allow_failure: false
