# `import` blocks for adopting UI-created authentik objects go here, next to the
# module whose resource addresses they name. Block shapes, the id per resource
# type, and the group-uniqueness caveat: README.md § Adoption and recovery.
#
# For a state-loss rebuild use ./import.sh, which imports the same address<->id
# table non-interactively and skips whatever is already in state.
