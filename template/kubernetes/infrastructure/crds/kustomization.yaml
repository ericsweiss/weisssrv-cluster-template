---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prometheus-operator-crds
resources:
  - namespace.yaml
  - release.yaml

# default-deny-ingress for the namespace; the `namespace:` transformer above
# places it. Free here — nothing runs in this namespace.
components:
  - ../../components/netpol-baseline
