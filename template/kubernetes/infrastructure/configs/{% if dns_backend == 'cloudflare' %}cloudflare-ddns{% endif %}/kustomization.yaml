---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cloudflare-ddns
resources:
  - namespace.yaml
  - cronjob.yaml

components:
  - ../../../components/netpol-baseline
  - ../../../components/netpol-egress-dns
  # Public only — the RFC1918 except-list is what makes this safe to run with a
  # write-capable DNS token.
  - ../../../components/netpol-egress-public
