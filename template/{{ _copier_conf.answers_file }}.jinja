# Generated by copier from weisssrv-cluster-template — do not edit by hand.
# `copier update` replays these answers against a newer template ref; changing a
# value here and re-running update is the supported way to change site data that
# copier baked in — see this repository's docs/RUNBOOKS.md § Updating the
# template. Which template that is, is `_src_path` below.
{{ _copier_answers | to_nice_yaml -}}
